# URL of the backend API (replace with your Render service URL in production)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Admin Password (should match backend .env)
# Note: Exposing passwords in frontend env vars is insecure for real apps.
# This is for MVP simplicity. Use proper auth systems in production.
NEXT_PUBLIC_ADMIN_PASSWORD="estrelas123"

# Firebase Config (Not needed anymore for core features, but keep if other Firebase services are used)
# NEXT_PUBLIC_FIREBASE_API_KEY=
# NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
# NEXT_PUBLIC_FIREBASE_PROJECT_ID=
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
# NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
# NEXT_PUBLIC_FIREBASE_APP_ID=
