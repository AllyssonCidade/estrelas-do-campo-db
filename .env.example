# PostgreSQL Connection String (provided by Render or your hosting)
DATABASE_URL="postgresql://user:password@host:port/database"

# Port for the backend server (Render sets this automatically, but good for local dev)
PORT=3001

# Static Admin Password for CMS actions
ADMIN_PASSWORD="estrelas123"
